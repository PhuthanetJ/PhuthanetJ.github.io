(function () { 'use strict'; if (!window.NT) return; NT.q('#ap-site-cards').innerHTML = NT.allowedSites().map(s => { const rows = NT_DATA['access-points'].filter(a => a.siteId === s.id), on = rows.filter(a => a.status === 'online').length; return '<a class="wt-card" href="ap-vendors.html?site=' + s.id + '"><h3>' + NT.esc(s.name) + '</h3><strong>' + rows.length + ' AP</strong><p>Online ' + on + ' · Offline ' + (rows.length - on) + '</p><span>ดูยี่ห้อและรุ่น →</span></a>'; }).join(''); })();
