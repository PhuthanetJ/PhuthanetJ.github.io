(function () { 'use strict'; const { q, esc } = NT; q('#login-accounts').innerHTML = NT_DATA.users.map(u => '<p><strong>' + esc(u.username) + '</strong> · ' + esc(NT_DATA.roles.find(r => r.id === u.role).name) + ' · ' + esc(u.siteIds.join(', ').toUpperCase()) + '</p>').join(''); q('#login-form').addEventListener('submit', e => { e.preventDefault(); try { WiFiAuth.login(q('#login-username').value, q('#login-password').value); location.replace('dashboard.html'); } catch (e) { q('#login-error').textContent = e.message; } }); })();
